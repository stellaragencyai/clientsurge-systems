/**
 * LazyImage — thin re-export of OptimizedImage for backward compatibility.
 * All new code should import OptimizedImage directly.
 */
export { default } from "@/components/performance/OptimizedImage";