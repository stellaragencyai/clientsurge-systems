// Canonical client portal context handler lives in main.ts.
// Keep this entry file as a thin compatibility wrapper so Base44 deployments
// cannot drift between entry.ts and main.ts implementations.
import './main.ts';
