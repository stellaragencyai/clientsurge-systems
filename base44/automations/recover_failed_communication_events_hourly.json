{
  "id": "recover_failed_communication_events_hourly",
  "name": "Recover failed communication events hourly",
  "trigger": {
    "type": "schedule",
    "cron": "0 * * * *"
  },
  "task": "Invoke recoverFailedCommunicationEvents with process_due=true and limit=1000. Record the returned summary. Alert an admin when dead_lettered > 0, manual_review > 0, or errors is non-empty.",
  "priority": "CRITICAL",
  "active": true
}
