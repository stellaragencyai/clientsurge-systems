name: Base44 Product Signup Hotfix Publish Superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Use Base44 Auto Publish instead
        run: |
          echo "This one-off product-signup publish workflow is superseded."
          echo "Use .github/workflows/base44-auto-publish.yml for GitHub -> Base44 production publishing and checkout proof."
